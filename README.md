# Telegram Rcon LOGGER 
- Установка зависимости: 
```bash
   py -m pip install -r requirements.txt
   ```

1. `cfg.py` - параметры
2. `get_id_group.py` - получить ID группы прописав `/start`
3. Сам логгер - `main.py`
